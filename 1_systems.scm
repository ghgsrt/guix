(define-module (1_systems)
  #:use-module (utils))
;  #:use-module (systems base)
 ; #:use-module (systems thinkpad))

;(load-systems)

;(display "systems bro")(newline)
;(define-syntax re-export-list
;  (syntax-rules ()
;    ((re-export-list lst)
;(begin
;(display "systems")(display lst)(newline)     
; (macroexpand `(re-export ,@lst))))))
;(re-export-list
;        (apply append (map
 ;                                       (lambda (inter)
 ;                                       (module-map (lambda (sym var) sym) inter))
  ;                                      (module-uses (current-module)))))

